"""Placeholder for future LLM-backed insight generation (summaries, Q&A, entity extraction).

Currently a no-op — this module exists so the package structure is ready for insight features
without requiring another layout change.
"""

from __future__ import annotations
