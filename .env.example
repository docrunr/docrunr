# RabbitMQ
RABBITMQ_HOST=rabbitmq
RABBITMQ_PORT=5672
RABBITMQ_USER=guest
RABBITMQ_PASSWORD=guest

# Storage
STORAGE_TYPE=local
STORAGE_BASE_PATH=/data

# S3-compatible storage (only when STORAGE_TYPE=s3)
S3_ENDPOINT=http://seaweedfs:8333
S3_ACCESS_KEY=docrunr
S3_SECRET_KEY=docrunr-secret
S3_BUCKET=docrunr
S3_REGION=us-east-1
S3_PORT=8333
SEAWEEDFS_MASTER_PORT=9333

# Worker (TXT extraction HTTP / bundled UI)
HEALTH_PORT=8080

# Host port published for worker-llm (container HTTP stays on 8080; map host 8081 → 8080)
LLM_HEALTH_PORT=8081
SQLITE_BASE_PATH=/db
JOB_TIMEOUT_SECONDS=120
WORKER_CONCURRENCY=1

# TXT extraction queues
RABBITMQ_QUEUE=docrunr.jobs
RABBITMQ_RESULT_QUEUE=docrunr.results
RABBITMQ_DLQ_QUEUE=docrunr.dlq

# LLM worker — LiteLLM proxy URL (worker-llm). In Docker Compose use the service hostname (set in docker-compose.llm.yml). For host-only dev (scripts/dev.mjs) use http://127.0.0.1:4000.
LITELLM_BASE_URL=http://litellm:4000
LITELLM_API_KEY=
LITELLM_TIMEOUT_SECONDS=600
RABBITMQ_LLM_QUEUE=docrunr.llm.jobs
RABBITMQ_LLM_RESULT_QUEUE=docrunr.llm.results
RABBITMQ_LLM_DLQ_QUEUE=docrunr.llm.dlq

# Ollama in Docker (docker-compose.ollama.yml): models pulled on container start (comma-separated).
# Names must match the ollama model tags used in litellm.yaml.
OLLAMA_EMBED_MODELS=nomic-embed-text,embeddinggemma,bge-m3,qwen3-embedding:8b

# Auth (optional)
UI_PASSWORD=

# Integration tests
DOCRUNR_INTEGRATION_STORAGE=local
DOCRUNR_INTEGRATION_S3_ENDPOINT=http://127.0.0.1:8333

# LLM integration test profile selection (test:integration:llm)
# Leave unset to pick randomly from the live LiteLLM /models list.
# INTEGRATION_LLM_PROFILES=nomic-embed-text-137m,embedding-gemma-300m,bge-m3-560m  # random pick from subset
